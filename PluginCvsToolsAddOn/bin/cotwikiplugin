#! /bin/bash
. SETTINGS

MODULE=$1

if [ "$MODULE" = "" ]
then
   echo "must specify a module"
   exit 1
fi

cd $TWIKISTAGE || echo "\$TWIKISTAGE not set - did you run from dir containing SETTINGS?"

cvs -z3 co twikiplugins/$MODULE
