%META:TOPICINFO{author="TWikiContributor" date="1111929255" format="1.0" version="$Rev$"}%
---+ Logout and Login
<!--

Have we got SessionPlugin?
%CALC{"$SET(IS_SESSION,$NOT($FIND(SESSIONVAR,%SESSIONVAR%)))"}%

Is the session authenticated by SessionPlugin?
%CALC{"$SET(IS_SESSION_AUTH,%SESSION_IS_AUTHENTICATED%)"}%

Have we got AuthPagePlugin?
%CALC{"$SET(IS_AUTH_PAGE,$FIND(AuthPagePlugin,$LISTIF($FIND(AuthPagePlugin,$item),%ACTIVATEDPLUGINS%)))"}%

Is REMOTE_USER set?
%CALC{"$SET(IS_AUTH,$NOT($EXACT(%REMOTE_USER%,)))"}%

-->

Your current ID is %WIKIUSERNAME%.

This TWiki seems to be using
%CALC{"$IF($GET(IS_AUTH_PAGE),%TWIKIWEB%.AuthPagePlugin. [[%SCRIPTURL%/login%SCRIPTSUFFIX%][Click here]] to log in,$IF($GET(IS_SESSION_AUTH),%TWIKIWEB%.SessionPlugin. To logout you need to close this browser and start a new one. If that doesn't work ask your administrator to delete CGI session %SESSIONID%,$IF($GET(IS_SESSION),%TWIKIWEB%.SessionPlugin. [[%SCRIPTURL%/viewauth%SCRIPTSUFFIX%/%WEB%/%TOPIC%][Click here]] to log in,$IF($GET(IS_AUTH),default authentication and you are already authenticated. Please restart your browser if you want to change your login.,default authentication and you are not logged in. [[%SCRIPTURL%/viewauth%SCRIPTSUFFIX%/%WEB%/%TOPIC%][Click here]] to login))))"}%

__Related Topics:__ TWikiUserAuthentication, UserDocumentationCategory, AdminDocumentationCategory
